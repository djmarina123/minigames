name: minigames_hub
description: Hub de jogos casuais para mobile.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.13.0
  firebase_auth: ^5.7.0
  firebase_analytics: ^11.6.0
  firebase_crashlytics: ^4.3.10
  flame: ^1.30.0
  provider: ^6.1.5
  shared_preferences: ^2.5.3
  google_mobile_ads: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
